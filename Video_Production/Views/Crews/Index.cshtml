@model IEnumerable<Video_Production.Models.Crew>

@{
    ViewData["Title"] = "Index";
}

<h2>Crew</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Producer)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Director)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ScriptWriter)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DP)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CameraOp)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Producer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Director)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScriptWriter)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CameraOp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
